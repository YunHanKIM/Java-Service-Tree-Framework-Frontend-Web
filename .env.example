# 선택 사항 — 세션 쿠키 서명 시크릿. 설정하지 않으면 개발용 기본값을 사용한다(실서비스 비권장).
# 프로덕션 배포 시에는 반드시 별도의 긴 무작위 문자열로 설정할 것.
SESSION_SECRET=

# AI API 키는 여기에 넣지 않는다 — 로그인 후 앱의 설정(/settings) 화면에서 등록한다.
# 서버 로컬 파일(.data/ai-settings.json, gitignored)에 저장된다.
