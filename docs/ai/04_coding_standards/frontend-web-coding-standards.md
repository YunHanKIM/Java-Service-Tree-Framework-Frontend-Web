# Frontend-Web — 코딩 규칙

- 기존 패턴에 맞춘다. 요청 없이 새 프레임워크·빌드 단계 도입 금지.
- JS는 unobtrusive·모듈화 유지. 인라인 핸들러 지양(코드베이스가 이미 쓰는 경우 제외).
- 시크릿·환경별 URL 하드코딩 금지 — 프로젝트 config/엔드포인트 규약 사용.
- 서버 측 검증과 정합되는 클라이언트 폼 검증, 필드 단위 에러 표시.
- 접근성 기본(레이블, 포커스, 필요 시 ARIA) 준수.

(세부 네이밍·파일 배치 규칙은 스캐폴딩 후 확정)
